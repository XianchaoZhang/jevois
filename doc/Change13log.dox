/*! \page Change13log JeVois 1.3 中的变化和新功能

JeVois 1.3 带来以下新功能：

- 新的标准化串行消息允许各种 JeVois 机器视觉模块和 Arduino 代码之间实现更高的互操作性。请参阅 \ref UserSerialStyle

- 新的主机命令 `jevois-add-videomapping` 用于将新的视频映射添加到 \b videomappings.cfg，检查是否有重复。现在，它用于新模块的 `postinstall` 脚本中。

- 新的主机命令“jevois-cmd”向 JeVois 发送简单命令并获取结果，而无需打开串行终端并将其连接到 JeVois。

- 新的主机命令 `jevois-cmd` 指示连接的 JeVois 相机开始/停止将其 microSD 导出为虚拟闪存驱动器。

- 新文档，包括 \ref UserLighting

- C++ 和 Python 的新教程

*/

